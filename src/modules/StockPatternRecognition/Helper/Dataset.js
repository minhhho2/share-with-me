class Dataset {
    constructor() {
        this.rawData = rawData.slice();
        this.parsedData = this.transformData(rawData.slice());
    }

    transformData = (rawData) => {

        var parsedData = [];

        return parsedData;

    }


}