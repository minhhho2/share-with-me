export default {
    add = (x, y) => {
        return x + y;
    },
    subtract = (x, y) => {
        return x - y;
    },
    // Cost function implemented as Dynamic Time Warping
    cost = (sample, pattern) => {
        // Calculate cost through cost function
        // if below threshold return yes
        const cost = 0;
        return cost;
    },
    costReduction = (sampleCost) => {
        // reduce for # of data points
        // reduce for matching number of 
    },
    classification = () => {
        // normalise sample
        // calculate cost between sample and pattern
        // label sample as a pattern if its below threshold
    }
}